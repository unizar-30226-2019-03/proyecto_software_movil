import { StyleSheet } from "react-native";

export default StyleSheet.create({
	container: {
		flex: 1
	},

	buttonAsignatura:{
		flex:1,
		color: '#000087',
		borderWidth: 1,
		borderColor: 'black'

	}
});
